export const api_information = {
    "url": "http://localhost:3000",
    "token": ""
};

// Versioned API base path. Bump this in one place when the backend cuts a new version.
export const api_v1_url = api_information.url + "/api/v1";